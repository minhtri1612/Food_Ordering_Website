{{!-- <div class="container mt-4">
  <div class="row g-4"> <!-- Added Bootstrap's gutter spacing for better spacing between cards -->
    {{#each item}}
    <div class="col-12 col-sm-6 col-md-4 col-lg-3"> <!-- Adjusted column sizes for better responsiveness -->
      <div class="card h-100 shadow-sm"> <!-- Added shadow and height utility for consistent card size -->
        <a href="/item/{{this.slug}}">
          <div class="card-img-container"> <!-- Added a container for the image -->
            <img src="{{this.image}}" alt="{{this.name}}" class="card-img-top"> <!-- Removed img-fluid -->
          </div>
        </a>
        <div class="card-body d-flex flex-column"> <!-- Flexbox for aligning content -->
          <a href="/item/{{this.slug}}" class="text-decoration-none">
            <h5 class="card-title text-lg font-semibold text-dark hover:text-primary transition-colors">{{this.name}}</h5>
          </a>
          <p class="card-text text-truncate" style="max-height: 3rem; overflow: hidden;">{{this.description}}</p> <!-- Truncated long descriptions -->
          <div class="mt-auto"> <!-- Push button to the bottom -->
            <a href="/login" class="btn btn-primary w-100">Chọn món</a> <!-- Full-width button -->
          </div>
        </div>
      </div>
    </div>
    {{/each}}
  </div>
</div>

<style>
  /* Ensure all images have the same size */
  .card-img-container {
    height: 200px; /* Set a fixed height for the image container */
    overflow: hidden; /* Hide any overflow */
  }

  .card-img-container img {
    width: 100%; /* Make the image take the full width of the container */
    height: 100%; /* Make the image take the full height of the container */
    object-fit: cover; /* Ensure the image covers the container while maintaining aspect ratio */
  }
</style> --}}


{{> hero}}

{{> exclusive-deals}}

{{> categories}}

{{> popular-restaurants}}

{{> about-us}}

